[package]
name = "bunny-agents"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
[dependencies]
bunny-core = { path = "../core" }
bunny-crypto = { path = "../crypto" }
tokio = { version = "1.43", features = ["full"] }
